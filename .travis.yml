language: php

sudo: required

php:
 - 5.6

services:
 - docker
 
before_install: composer global require fxp/composer-asset-plugin -vvv

 
install:
 - composer self-update
 - composer install

script:
 - composer test